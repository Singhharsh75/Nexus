# Resume from saved state

Read the following files in order:
1. `CLAUDE.md` — project conventions and rules
2. `SPEC.md` — full project specification
3. `STATUS.md` — where we left off last session

Then:
1. Summarize what's already done and what remains
2. Run `pnpm typecheck` and `pnpm test` to verify the existing code still works
3. Ask me to run `/usage` before starting the next sub-task
4. Wait for my go-ahead before writing any code

Do NOT re-implement anything that STATUS.md says is already complete.
